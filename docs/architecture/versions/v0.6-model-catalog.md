# 安安虎工伤智能助手 Agent Harness 技术架构（MVP v0.6）

## 版本信息

| 字段 | 内容 |
|---|---|
| 状态 | 当前有效架构 |
| 版本 | v0.6 |
| 发布日期 | 2026-07-10 |
| 基线版本 | v0.5-real-model-gateway |
| 变更原因 | 在 OpenAI-compatible ModelGateway 基础上引入 providers + profiles 两层模型目录，支持多 provider 和按 Agent/Profile 路由。 |

## 完整架构

系统仍是工伤咨询领域 CLI Agent Harness。CLI 与 Eval 只依赖框架中立
`WorkflowRuntime`；默认实现为 `LangGraphWorkflowRuntime`，Native 实现保留用于回归。
两种 Runtime 共享阶段服务、项目 reducer、CapabilityGateway、ModelGateway、TraceEvent 和
Eval 数据，不持有 provider SDK 类型。

```text
NativeWorkflowRuntime -----|
                           +-> application stage -> ModelGateway
LangGraphWorkflowRuntime --|                         |
                                                     +-> FakeModelGateway
                                                     +-> OpenAICompatibleModelGateway
                                                            |
                                                            +-> /chat/completions

ModelRequest -> ModelResult | ModelGatewayError
                       |
                       +-> project trace + UsageRecord fields
```

业务阶段保持 `understand -> merge_facts -> validate_facts -> clarify |
resolve_jurisdiction -> plan -> execute -> validate_evidence -> compose -> safety -> complete`。
本版本只替换意图识别阶段的模型边界，不改变业务状态机、reducer、CapabilityGateway 或
Native/LangGraph 的调度职责。

## ModelGateway 协议

公共端口使用项目 Pydantic 类型并提供 async `generate_structured()`。`ModelRequest` 至少携带
运行关联 ID、node、logical call、模型 profile、Prompt 引用和渲染后文本；`ModelResult`
包含结构化输出、provider、model、finish reason、token usage、估算费用、延迟和尝试次数。
供应商响应、HTTP 客户端和 SDK 类型不得进入 Agent、application 或 Runtime 公共协议。
结构化输出使用项目提供的 JSON Schema Draft 2020-12 校验，类型、枚举、范围和嵌套结构不合法时
统一返回 `output_schema_error`，不得先记录为成功模型调用。

错误统一归类为配置、鉴权、限流、超时、provider、响应格式和输出 schema 错误。错误对象不得
包含 API key 或完整敏感响应体。是否重试由 gateway policy 决定，logical call ID 在物理重试间
保持不变，attempt 单独增加。

## Provider/Profile 模型目录

真实适配器继续使用 OpenAI-compatible `POST /chat/completions`，采用 Bearer token、JSON object
结构化输出和 provider usage 字段。模型配置拆为两层：

- `providers` 描述协议、base URL 和 API key 环境变量名。
- `profiles` 描述业务可选择的模型档位，引用 provider，并声明 model、temperature、timeout 和可选价格。

Agent、Prompt、Runtime 公共协议只使用 `model_profile`。provider 名称、base URL、API key 和 SDK 类型
不得进入 domain、application、Agent 或 Runtime 公共协议。`ModelRouter` 负责解析 profile、关联 provider
并装配 gateway；同一 profile 的 gateway 在进程内缓存。

真实 smoke 必须同时显式设置启用开关、API key、base URL 和真实模型 profile。常规 contract
tests 使用 Fake 或受控 HTTP transport；未启用 smoke 时测试 skip，而不是失败。禁止把 key 写入
profile、trace、usage artifact、测试 fixture 或版本库。

## 配置优先级

`ANANHU_MODEL_CATALOG` 指向 YAML 模型目录时优先使用目录；未配置目录时兼容 v0.5 的
`ANANHU_MODELS`、`ANANHU_MODEL_API_KEY` 和 `ANANHU_MODEL_BASE_URL`。目录文件只能保存
`api_key_env` 引用，密钥明文仍只存在于对应环境变量中。默认 legacy profile 继续使用 Fake，
因此未设置目录与密钥时不会访问网络。

## Trace 与 Usage

模型调用写入项目 `model_started`、`model_finished` 或 `model_failed` 事件，并关联 run、request、
runtime、node、logical call、attempt、profile、provider、model 和 Prompt 版本。成功 usage 至少区分
input、output、cache 和 total token，费用只在配置了价格时估算，并明确币种。RunReport 汇总项目
ModelResult，不依赖 provider SDK 或 LangSmith 计费口径。

Fake 与真实调用通过 `usage_source=fake|provider` 分开报告。Fake token 和费用固定为零，只服务确定性
回归；真实 smoke 结果不得与离线 eval 汇总为同一质量指标。

## 相对 v0.5 的差异

1. 新增 `providers + profiles` 两层模型目录和启动期配置校验。
2. 支持多个 OpenAI-compatible provider，并按业务 `model_profile` 装配 gateway。
3. API key 由 provider 的 `api_key_env` 间接引用，不进入目录 profile 或公共协议。
4. 保留 v0.5 单 provider 环境变量配置作为兼容路径。
5. ModelGateway、Trace、Usage 和 Native/LangGraph 运行时协议保持不变。

## 兼容性与迁移策略

默认配置仍为 Fake，因此现有 CLI、离线 eval 和双运行时差分结果保持确定性。未配置
`ANANHU_MODEL_CATALOG` 时由兼容加载器把 v0.5 配置转换为内存目录；旧 `ModelRouter` profile 查询
语义保持兼容。迁移时先复制 `config/models.example.yaml` 为本地 `config/models.yaml`，再分别设置
provider 所引用的密钥环境变量。历史 trace 不原地迁移。

## 任务计划与验收标准

实施入口为 `docs/superpowers/plans/archive/2026-07-10-model-catalog.md`。验收要求目录解析与非法配置测试通过；
ModelRouter 可按 profile 装配 Fake 或 OpenAI-compatible gateway；v0.5 配置继续兼容；无 key 的 CI 不失败；
Native/LangGraph 差分保持等价；目录、trace、fixture 和 artifact 不包含密钥明文。

## 已知限制

OpenAI-compatible 方言在结构化输出、usage 和错误体上存在差异，本版本只承诺已定义的最小兼容
子集。目录暂不支持自动 fallback、动态热加载、密钥管理服务或非 OpenAI-compatible 协议。真实 smoke
只验证连接、协议解析和证据记录，不证明模型业务质量。真实 KnowledgeGateway、完整咨询 smoke、
生产级重试/fallback、流式输出和集中价格目录仍属于后续任务。
